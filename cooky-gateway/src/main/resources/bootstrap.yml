spring:
  application:
    name: cooky-gateway
  cloud:
    nacos:
      config:
        server-addr: ${nacos.url}:8848
        group: DEFAULT_GROUP
        prefix: cooky-gateway
        file-extension: yaml
      discovery:
        server-addr: ${nacos.url}:8848
